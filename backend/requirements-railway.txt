# ============================================================
# Railway Slim Requirements
# API-only deployment — no torch, no local ML models
# ============================================================

# --- Core Web Framework ---
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
python-multipart==0.0.21
itsdangerous==2.2.0
starlette-compress==1.6.1

# --- Auth & Security ---
python-socketio==5.16.0
python-jose==3.5.0
cryptography
bcrypt==5.0.0
argon2-cffi==25.1.0
PyJWT[crypto]==2.10.1
authlib==1.6.6

# --- HTTP Clients ---
requests==2.32.5
aiohttp==3.13.2
async-timeout
aiocache
aiofiles
httpx[socks,http2,zstd,cli,brotli]==0.28.1
starsessions[redis]==2.2.1
python-mimeparse==2.0.0

# --- Database ---
sqlalchemy==2.0.45
alembic==1.17.2
peewee==3.18.3
peewee-migrate==1.14.3
pycrdt==0.12.44
redis

# --- Task Scheduling ---
APScheduler==3.11.2
RestrictedPython==8.1

# --- Logging & Utilities ---
loguru==0.7.3
asgiref==3.11.0
chardet==5.2.0
validators==0.35.0
psutil
pydub

# --- AI / LLM Clients (API-only, no local inference) ---
tiktoken
mcp==1.25.0
openai
anthropic
google-genai==1.56.0

# --- LangChain (text splitting + document processing only) ---
langchain==1.2.0
langchain-community==0.4.1
langchain-classic==1.0.1
langchain-text-splitters==1.1.0

# --- Document Parsing (lightweight) ---
pypdf==6.5.0
fpdf2==2.8.5
pymdown-extensions==10.20
docx2txt==0.9
python-pptx==1.0.2
Markdown==3.10
pandas==2.3.3
openpyxl==3.1.5
xlrd==2.0.2
fake-useragent==2.2.0

# --- Vector DB (Chroma only — smallest footprint) ---
chromadb==1.4.0

# --- Code formatting (used by backend) ---
black==25.12.0

# --- YouTube / web search ---
youtube-transcript-api==1.2.3
ddgs==9.10.0

# NOTE: The following are intentionally EXCLUDED to keep image small:
# - torch, torchvision, torchaudio       (~2GB) — not needed, using external LLM APIs
# - sentence-transformers, transformers  (~500MB) — not needed, no local embeddings
# - faster-whisper, onnxruntime          (~300MB) — not needed, using Soniox/Deepgram STT
# - opencv-python-headless, pillow       (~150MB) — not needed for API-only
# - playwright                           (~300MB) — not needed
# - pymilvus, qdrant-client, weaviate    (~200MB) — only Chroma is included above
# - azure-*, google-cloud-*, boto3       (~300MB) — not needed
# - accelerate, einops, sentencepiece    (~100MB) — not needed without local models
